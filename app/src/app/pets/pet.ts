export interface Pet {
  id: number | undefined,
  revision: number | undefined,
  name: string | undefined,
  additionalInfo: string | undefined,
  userId: number | undefined,
  userRevision: number | undefined
}
